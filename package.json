{
  "name": "habit-checkin",
  "version": "0.1.0",
  "private": true,
  "description": "习惯打卡 - 每日打卡原型",
  "scripts": {
    "sync": "cap sync android",
    "build:android": "cd android && ./gradlew assembleDebug"
  },
  "dependencies": {
    "@capacitor/android": "^7.0.0",
    "@capacitor/core": "^7.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^7.0.0"
  }
}
